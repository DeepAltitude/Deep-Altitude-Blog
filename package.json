{
  "name": "deepaltitude",
  "description": "DeepAltitude \u2014 public writing and a private daily notebook.",
  "dependencies": {
    "@astrojs/cloudflare": "12.6.12",
    "@astrojs/rss": "4.0.14",
    "astro": "5.16.9",
    "typescript": "5.9.3",
    "yaml": "2.9.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "wrangler": "4.88.0"
  },
  "engines": {
    "node": ">=22"
  },
  "private": true,
  "scripts": {
    "astro": "astro",
    "build": "node scripts/build.mjs",
    "cf-typegen": "wrangler types",
    "check": "astro check && tsc --noEmit && npm test && npm run build && node notebook/verify.mjs && wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "dev": "astro dev",
    "preview": "npm run build && wrangler dev",
    "test": "node notebook/test-editor.mjs && node notebook/test-system.mjs && node notebook/test-calendar.mjs",
    "test:runtime": "node notebook/test-routes.mjs"
  },
  "type": "module"
}
