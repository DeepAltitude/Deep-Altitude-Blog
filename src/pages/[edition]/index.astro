---
import {editions} from '../../utils/posts';
import Home from '../../components/Home.astro';
export function getStaticPaths(){return editions.map(edition=>({params:{edition},props:{edition}}));}
const {edition}=Astro.props;
---
<Home edition={edition}/>
