---
import { domains, domainNames } from "../utils/domains";
---

<nav class="topic-nav" aria-label="Filtruoti pagal sritį" data-domain-filter>
  <a href="/uzrasai/" data-filter="" aria-current="page">
    Visi
  </a>
  {domains.map((domain) => (
    <a href={`/uzrasai/?domain=${domain}`} data-filter={domain}>
      {domainNames[domain]}
    </a>
  ))}
</nav>
